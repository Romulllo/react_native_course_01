import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const ImageDatail = (props) => {
  return <Text>{props.title}</Text>;
};

const styles = StyleSheet.create({});

export default ImageDatail;